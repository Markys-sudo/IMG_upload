# nginx.conf
server {
    listen 80;

    # Статичні файли
    location /images/ {
        alias /images/;
        autoindex on;
    }

    # Проксі на Python-бекенд для інших маршрутів
    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
